# Прямые зависимости проекта с зафиксированными версиями.
# Фиксируем точные версии, чтобы у тебя, у заказчика и на сервере
# собиралось одно и то же — «а у меня работало» не должно случаться.
#
# Установка:  .venv\Scripts\python.exe -m pip install -r requirements.txt

# Фреймворк Telegram-бота: роутеры, FSM, клавиатуры.
aiogram==3.30.0

# Чтение и валидация настроек из .env в типизированный объект.
pydantic-settings==2.15.0

# Чтение YAML-конфига с направлениями обучения.
PyYAML==6.0.3

# Разбор и нормализация телефонов (порт Google libphonenumber).
phonenumbers==9.0.36

# Асинхронный доступ к SQLite (обычный sqlite3 блокирует event loop).
aiosqlite==0.22.1

# Работа с Google Sheets. Тянет за собой google-auth для авторизации
# по ключу сервисного аккаунта.
gspread==6.2.1

# База часовых поясов для zoneinfo. В Linux и macOS она есть в системе,
# а в Windows и в slim-образах Docker — нет. Без неё падает
# ZoneInfo("Europe/Moscow"), поэтому ставим явно.
tzdata==2026.3

# Дальше по фазам добавим:
#   pytest        — тесты                          (Фаза 8)
